import { ChangeBackgroundDirectiveDirective } from './change-background-directive.directive';

describe('ChangeBackgroundDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeBackgroundDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
