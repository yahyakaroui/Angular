import { DatePersonnaliseePipe } from './date-personnalisee.pipe';

describe('DatePersonnaliseePipe', () => {
  it('create an instance', () => {
    const pipe = new DatePersonnaliseePipe();
    expect(pipe).toBeTruthy();
  });
});
